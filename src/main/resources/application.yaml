server:
  port: 8085

spring:
  datasource:
    url: jdbc:h2:mem:modulith_db
    username: sa
    password:
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop